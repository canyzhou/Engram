{
  "name": "engram",
  "version": "0.6.2",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "npm --prefix server run start:local",
    "build:extension": "node scripts/build-extension.mjs",
    "test": "node --test tests/*.test.mjs server/test/*.test.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
